import express from "express";
const Review = express.Router();

// Review.post("/books/:bookId/reviews", addReview);
// Review.get("/books/:bookId/reviews", getAllReviews);
// Review.delete("/reviews/:id", deleteReviewByID);

export default Review;
