import express from "express";

const Books = express.Router();

// Books.post("/books", addBooks);
// Books.get("/books", getAllBooks);
// Books.get("/books/:id", getBookByID);
// Books.put("/books/:id", updateBookByID);
// Books.delete("/books/:id", deleteBookByID);

export default Books;