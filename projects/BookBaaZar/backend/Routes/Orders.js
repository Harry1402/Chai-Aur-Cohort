import express from "express";

const Orders = express.Router();

// Orders.post("/orders",PlaceOrder)
// Orders.get("/orders",getAllOrders)
// Orders.get("/orders/:id",getOrderByID)

export default Orders;
